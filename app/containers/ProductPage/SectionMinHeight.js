import Section from 'components/Containers/Section'

const SectionMinHeight = Section.extend`
    min-height: 20em;
    background: rgba(255,255,255,0.5);
    color: #000;
    padding: 0.5em 1em;
`;

export default SectionMinHeight;